**Задача 1. Написать функцию `count_positive(numbers)` которая принимает список чисел и возвращает, сколько из них положительные.**

**Задача 2. Написать функцию `count_values_above_threshold(data, threshold)`, которая принимает словарь с числовыми значениями и порог, возвращая количество элементов со значением > threshold.**

**Задача 3. Дан список словарей с информацией о студентах (имя, баллы). Написать функцию `filter_students(students, min_score)` которая возвращает список имён студентов с баллом >= min_score.**

**Задача 4. Написать функцию `group_by_category(items)` которая принимает список словарей с ключом "category" и группирует элементы по категориям в словарь.**
