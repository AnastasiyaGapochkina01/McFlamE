# I Код с занятия
<details>
  <summary>CSV</summary>
  
  ```python
  import csv

def save_scores(filename, scores):
    with open(filename, 'a', encoding='utf-8') as f:
        writer = csv.writer(f, delimiter=';')
        writer.writerow(["Name", "Score"])
        for name, score in scores:
            writer.writerow([name, score])

def load_scores(filename):
    with open(filename, 'r', encoding='utf-8') as f:
        reader = csv.reader(f, delimiter=';')
        next(reader)
        scores = []
        for row in reader:
            scores.append((row[0], row[1]))
    return scores
    #print(scores)


scores = [
    ("admin", 1500),
    ("user1", 100),
    ("user2", 345)
]

loaded_res = load_scores("results.csv")
print(loaded_res)
#save_scores("results.csv", scores)
  ```
</details>

<details>
  <summary>JSON</summary>
  
  ```python
  import json

settings = {
    "sound": True,
    "level": "middle",
    "fps": 60,
    "resolution": [1920, 1080]
}
filename = "conf.json"
with open(filename, 'w', encoding='utf-8') as f:
    json.dump(settings, f, indent=2)

with open(filename, 'r', encoding='utf-8') as f:
    current_settings = json.load(f)

for key, value in current_settings.items():
    print(f"{key}: {value}")

  ```
</details>

# II Задачи
1. В CSV-файле `scores.csv` хранятся строки в формате
```csv
player,score
Alice,1200
Bob,950
Eve,1430
Charlie,1100
David,1020
Frank,870
Grace,1340
Heidi,990
Ivan,1280
Judy,1150
```
Написать функцию, которая выведет игрока с наибольшим количеством очков

2. Написать функцию, которая сгенерирует новго игрока со случайным количеством очком (от 100 до 2500)
3. Написать функцию, которая в тот же файл `scores.csv` дозапишет сгенерированного в предыдущей функции игрока (например `"Max", 1600`)
4. Файл `weapons.csv` содержит данные
```csv
name,damage,weight,price
Sword,25,5,150
Axe,30,8,180
Bow,15,2,120
Dagger,12,1,80
Mace,35,10,200
Spear,22,6,140
Crossbow,18,4,160
Halberd,28,9,210
Hammer,32,11,230
Staff,14,3,100
```
Написать
- функцию, которая выведет самое легкое и самое тяжелое оружие
- функцию, которая выведет стоимость конкретного оружия (запросить у пользователя название)

5. В файле `player.json` хранится информация об игроке
```json
{
    "name": "Alice",
    "level": 3,
    "inventory": ["sword", "potion", "shield"]
}
```
Написать функцию, которая спользуя данные из `player.json` увеличит уровень персонажа на 1 и сохранит изменения обратно в файл.

6. В файле `enemies.json` хранится информация о вражеских персонажах
```json
[
    {"type": "goblin", "hp": 30},
    {"type": "orc", "hp": 70},
    {"type": "troll", "hp": 120},
    {"type": "skeleton", "hp": 25},
    {"type": "zombie", "hp": 40},
    {"type": "bandit", "hp": 55},
    {"type": "ogre", "hp": 150},
    {"type": "dragon", "hp": 300},
    {"type": "slime", "hp": 20},
    {"type": "vampire", "hp": 90}
]
```
Написать функцию, которая выведет всех врагов, у которых hp больше 50
